# 1. 의존성 및 빌드 결과물 (이미지 내에서 npm install & build 하므로 무시)
node_modules/
dist/
.eslintcache

# 2. 소스 코드 관리 도구
.git/
.gitignore

# 3. 환경 설정 파일 (보안 및 환경별 차이 때문)
.env
.env*.local

# 4. IDE 및 설정 파일
.idea/
.vscode/

# 5. 도커 관련 파일 자체
Dockerfile
.dockerignore
nginx.conf  # (참고) COPY nginx.conf 명령어로 명시적으로 복사하므로 무시해도 무방
